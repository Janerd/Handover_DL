# 仿真实现过程说明

---

## 一、仿真框架概述

本研究采用 NVIDIA Sionna RT（Ray Tracing）v2.0.1 作为物理层仿真引擎，在真实城市场景中生成 UE 移动轨迹的多径信道数据，用于训练和评估切换决策算法。整个仿真流程分为三个阶段：场景与网络配置、UE 轨迹生成、信道仿真与特征提取。

---

## 二、Sionna RT 的优势与局限

### 2.1 优势

**物理精确性**

Sionna RT 基于 Mitsuba 3 渲染引擎，采用蒙特卡洛射线追踪方法计算电磁波传播。与统计信道模型（如 3GPP TR 38.901）相比，射线追踪能够精确建模建筑物反射、衍射和透射，生成与真实环境高度吻合的多径信道。每条路径的时延、到达角、多普勒频移均由物理定律直接推导，而非统计分布采样。

**场景几何感知**

射线追踪天然感知建筑物的三维几何结构。当 UE 绕过建筑物角落时，LOS/NLOS 状态会发生突变，RSRP 和 SINR 的变化模式与真实网络一致。这使得仿真数据能够反映城市环境中切换决策的真实挑战，而统计模型无法捕捉这种空间相关性。

**射线追踪专有特征**

Sionna RT 能够提取统计模型无法提供的信道特征，包括：Ricean K 因子（直接反映 LOS/NLOS 状态）、最短路径时延（UE 与基站距离的代理指标）、RMS 时延扩展（多径丰富程度）。这些特征为深度学习模型提供了比 RSRP/RSRQ 更丰富的信道状态信息。

**可复现性**

给定相同的场景几何、基站位置和 UE 轨迹，射线追踪结果完全确定，不存在统计模型的随机性。这保证了实验结果的可复现性，便于算法对比。

### 2.2 局限

**计算开销大**

每个时隙对每个 UE 位置独立执行射线追踪，计算量远大于统计模型。本实验生成 120 条轨迹（约 26 万个时隙）耗时约 298 分钟，而统计模型通常在秒级完成。这限制了数据集规模和快速迭代的能力。

**时隙间独立性**

每个时隙的射线追踪独立计算，相邻时隙之间没有显式的路径连续性。在真实信道中，多径分量是连续演化的（同一散射体在相邻时隙之间只是略微改变角度），而仿真中每次重新采样射线。这对多普勒频移的精度有一定影响，但对 RSRP 和 K 因子的影响较小。

**场景静态性**

仿真场景中的建筑物和散射体是静态的，不考虑行人、车辆等动态散射体的影响。真实城市环境中，动态散射体会引入额外的多普勒扩展和信道时变性。

**天线模型简化**

本实验采用 SISO 全向天线，不考虑实际基站的定向天线和波束赋形。真实 5G 网络中，天线方向图对覆盖范围和切换边界有显著影响。

---

## 三、场景选取要素

### 3.1 慕尼黑市中心场景的选择依据

**真实城市几何**

慕尼黑市中心场景基于真实的 OpenStreetMap 数据构建，包含密集的欧洲城市街道网格和建筑物。建筑物高度 10-30m，街道宽度 10-20m，建筑物占地约 43.2%。这种密集城区（Dense Urban）场景是 5G 切换研究的典型场景，也是 3GPP 标准化测试的重要场景类型。

**弱覆盖特性**

慕尼黑场景中，建筑物遮挡导致大量 NLOS 区域，UE 在街道转弯时会经历 LOS/NLOS 突变。这正是切换决策的关键挑战：A3 算法基于 RSRP 的决策在遮挡场景下容易产生误判，而深度学习模型可以利用 K 因子等特征识别遮挡状态。

**Sionna 内置支持**

慕尼黑场景是 Sionna RT 的官方内置场景，经过优化处理（去除重复顶点、简化几何），可直接加载使用，无需额外的场景预处理工作。

### 3.2 基站部署策略

本实验采用 7 个基站的非均匀布局，站间距约 307m，基站高度 10m（模拟路灯杆部署）。非均匀布局的设计考虑如下：

基站位置通过 Sionna RT GUI 在 3D 场景中手动放置，确保基站位于街道上而非建筑物内，且覆盖范围能够覆盖主要街道。这种部署方式比六边形网格更接近真实网络的不规则部署，能够产生更多样化的切换场景。

7 个基站的规模在计算可行性和场景复杂度之间取得平衡：基站数量足够产生多次切换事件（每条轨迹平均 4-9 次切换），同时射线追踪的计算量在可接受范围内。

### 3.3 场景范围与边界处理

仿真场景覆盖 1km × 1km（坐标范围 ±500m），UE 轨迹限制在场景内部（距边界至少 30m），避免边界效应。场景边缘建筑物密集，信号覆盖较弱，不适合作为轨迹起点。

---

## 四、数据选取与格式

### 4.1 UE 轨迹设计

**速度分级**

轨迹按速度分为三组：30 km/h（行人/自行车）、60 km/h（城市车辆）、120 km/h（快速路车辆），每组 40 条，共 120 条。速度分级的目的是覆盖不同的切换频率和多普勒特性：低速 UE 切换频率低但停留时间长，高速 UE 切换频率高但每次切换的决策窗口短。

**轨迹类型多样性**

每种速度包含多种轨迹类型：慕尼黑街道行走（沿主要街道方向移动，频繁转弯）、网格街道（沿水平/垂直方向移动，在路口转弯）、圆弧转弯（模拟匝道/环岛）、停留-移动交替（模拟等红灯）。多样化的轨迹类型确保数据集覆盖不同的移动模式，提高模型的泛化能力。

**轨迹长度**

轨迹长度根据速度自适应调整，目标覆盖约 4 个站间距（约 1200m）的距离，确保每条轨迹经历多次切换事件。30 km/h 轨迹约 3680 个时隙（约 147 秒），120 km/h 轨迹约 920 个时隙（约 37 秒）。

**可行走约束**

为确保轨迹起点和路径位于街道而非建筑物内，首先从 Sionna 场景的 Mitsuba 网格中提取建筑物占地面积，生成分辨率为 2m 的可行走网格。轨迹起点从可行走区域中采样，中高速轨迹（>30 km/h）在每个时隙检测前方是否为建筑物，通过沿墙滑行或转向机制绕过障碍。

### 4.2 信道仿真参数

射线追踪参数设置为：最大反射次数 3 次，最大衍射次数 1 次，每个发射机的采样射线数 $10^6$。这些参数在计算精度和速度之间取得平衡：3 次反射能够捕捉主要的多径分量，$10^6$ 条射线保证了统计收敛性。

为加速仿真，利用邻区关系配置（每个基站平均 3.4 个邻区）减少射线追踪的基站数量，相比全量计算加速约 1.6 倍。

### 4.3 数据格式

**轨迹级数据（trajectory_data.npz）**

存储每条轨迹的完整时序数据，包括：UE 位置序列 $[T, 2]$、各基站的 RSRP/RSRQ/SINR/Doppler/K_factor/DelaySpread/min_tau 序列 $[T, C]$。这种格式保留了完整的时序信息，用于切换策略的回放评估。

**样本级数据（dataset.npz）**

将轨迹数据切分为固定长度的样本，每个样本包含：特征窗口 $\mathbf{X} \in \mathbb{R}^{10 \times 70}$（过去 10 个时隙的 70 维特征）和标签 $Y \in \{0,\ldots,6\}$（切换收益最大的目标小区）。这种格式适合批量训练深度学习模型。

**特征归一化**

所有特征在存储前进行归一化：RSRP 和 RSRQ 采用 min-max 归一化，SINR 归一化到 $[-20, 40]$ dB，Doppler 频移对称归一化到 $\pm 500$ Hz，K 因子和时延类特征正值归一化。归一化参数固定，确保训练和推理时的特征分布一致。

### 4.4 数据集划分

按轨迹进行分层划分，训练集/验证集/测试集比例为 60:20:20，分别包含 72/24/24 条轨迹。分层划分确保每种速度在各划分中的比例相同（每种速度各 24/8/8 条）。最终数据集包含约 25.6 万个样本。

---

## 五、仿真流程总结

```
1. 场景配置
   ├── 加载慕尼黑 3D 场景（Sionna RT 内置）
   ├── 手动放置 7 个基站（Sionna RT GUI）
   └── 配置邻区关系（手动优化）

2. 轨迹生成
   ├── 生成可行走网格（从建筑物网格提取）
   ├── 生成 120 条 UE 轨迹（3 种速度 × 多种类型）
   └── 碰撞检测（中高速轨迹避开建筑物）

3. 信道仿真（约 298 分钟）
   ├── 每时隙独立射线追踪
   ├── 提取 10 类信道特征
   └── L3 滤波（RSRP 指数加权平均）

4. 数据集构建
   ├── 特征归一化
   ├── 切换收益标签生成
   └── 训练/验证/测试集划分