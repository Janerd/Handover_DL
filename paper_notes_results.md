# 实验结果分析

---

## 一、训练结果

### 1.1 分类性能

三种深度学习模型在测试集上的分类性能如下：

| 模型 | 验证集准确率 | 测试集准确率 | Macro-F1 | 训练时间 |
|---|---|---|---|---|
| GRU | 94.5% | 94.8% | 93.7% | 44s |
| TCN | 94.5% | 95.4% | 94.0% | 77s |
| Transformer | 94.9% | 95.3% | 94.1% | 151s |

三种模型的分类准确率均超过 94%，Macro-F1 超过 93%，说明模型能够有效学习信道特征与最优切换目标之间的映射关系。Transformer 收敛最慢（32 个 epoch），但最终达到最高的验证集准确率（94.9%）。

**标签不均衡的影响**：测试集中 cell_0 和 cell_2 各占约 31%，cell_1 和 cell_3 各占约 5-6%。通过类别加权损失函数，三种模型对少数类的 F1 分数均超过 90%，说明加权策略有效缓解了标签不均衡问题。

---

## 二、切换策略评估结果

### 2.1 总体对比

在测试集 24 条轨迹上的评估结果：

| 算法 | 均值 SINR [dB] | P5 SINR [dB] | 切换次数 | RLF 次数 | 乒乓率 | 推理时延 |
|---|---|---|---|---|---|---|
| A3 (optimized) | +9.14 | -26.08 | **2.8** | **151** | **0.6%** | N/A |
| GRU | +9.06 | -26.08 | 8.0 | 162 | 16.7% | 572 μs |
| TCN | +9.23 | -26.08 | 6.6 | 160 | 6.1% | 2284 μs |
| **Transformer** | **+9.36** | -26.08 | 6.7 | 153 | 12.3% | **575 μs** |

### 2.2 按速度分组

| 算法 | 30 km/h | 60 km/h | 120 km/h |
|---|---|---|---|
| A3 | +9.03 dB | +17.62 dB | -7.40 dB |
| GRU | +8.90 dB | +17.95 dB | -8.03 dB |
| TCN | +9.07 dB | +17.92 dB | -7.51 dB |
| Transformer | +9.22 dB | +17.99 dB | -7.37 dB |

---

## 三、结果分析

### 3.1 均值 SINR 分析

**Transformer 在均值 SINR 上超过 A3 基线 0.22 dB**（+9.36 vs +9.14 dB），GRU 略低于 A3（+9.06 dB），TCN 介于两者之间（+9.23 dB）。

深度学习模型的 SINR 优势来源于其对历史信道序列的利用。A3 算法仅基于当前时刻的 RSRP 做决策，而深度学习模型能够利用过去 10 个时隙（400ms）的信道变化趋势，在 RSRP 开始下降时提前预判切换时机，避免在信号质量已经较差时才触发切换。

**P5 SINR 四种算法完全一致**（-26.08 dB），说明在最差 5% 的时刻，切换策略的差异对覆盖质量没有影响。这些极端情况主要由建筑物深度遮挡导致，任何切换策略都无法避免。

**120 km/h 场景下均值 SINR 为负值**（约 -7 至 -8 dB），这是因为高速 UE 在场景边缘（BS4 覆盖区域，坐标 (-548, -197)，超出场景边界）停留时间较长，该区域信号覆盖极弱。这一现象与基站部署的非均匀性有关，而非算法本身的问题。

### 3.2 切换次数分析

**A3 的切换次数（2.8 次/轨迹）远少于深度学习模型（6.6-8.0 次/轨迹）**，这是 A3 在切换效率上的核心优势。

A3 的低切换次数源于其 TTT 机制：条件必须持续满足 80ms 才触发切换，天然过滤了短暂的信号波动。深度学习模型虽然采用了滑动窗口（连续 3 次预测才切换，等效 120ms），但由于模型预测的是"未来 400ms 的最优小区"，对信道变化更敏感，导致切换更频繁。

**TCN 的切换次数（6.6 次）少于 GRU（8.0 次）和 Transformer（6.7 次）**，说明 TCN 的切换决策更保守。这可能与 TCN 的卷积结构有关：因果膨胀卷积对局部时序模式的建模更平滑，对短暂波动的响应不如 GRU 和 Transformer 敏感。

### 3.3 乒乓率分析

**A3 的乒乓率（0.6%）远低于深度学习模型（6.1-16.7%）**，这是 A3 切换稳定性的体现。

乒乓切换（1s 内来回切换）主要发生在小区边界附近，信号强度相近的两个基站之间。A3 的迟滞量（2 dB）和 TTT 机制共同抑制了乒乓切换。深度学习模型的乒乓率较高，说明模型在边界区域的预测不够稳定，容易在相邻时隙预测不同的目标小区。

**TCN 的乒乓率（6.1%）显著低于 GRU（16.7%）和 Transformer（12.3%）**，接近 A3 的水平。这表明 TCN 的切换决策在时间上更一致，不容易在相邻时隙产生矛盾的预测。

### 3.4 RLF 次数分析

**四种算法的 RLF 次数相近**（151-162 次），说明切换决策对 RLF 的影响有限。RLF 主要由信道本身决定（建筑物深度遮挡、场景边缘弱覆盖），而非切换策略的差异。

A3 的 RLF 次数（151 次）略少于深度学习模型（153-162 次），这与 A3 的切换次数少有关：更少的切换意味着更少的切换中断期，从而减少了切换过程中可能发生的 RLF。

### 3.5 推理时延分析

**Transformer 的推理时延（575 μs）与 GRU（572 μs）相近，均远低于 TCN（2284 μs）**。

TCN 推理时延高的原因是 BatchNorm 层在推理时需要使用训练集统计量（running mean/var），且卷积操作的内存访问模式不如矩阵乘法高效。GRU 和 Transformer 的推理时延均在 600 μs 以内，满足 UE 侧实时决策的时延要求（切换决策需在一个时隙 40ms 内完成，600 μs 仅占 1.5%）。

---

## 四、RSRP-SINR 相关性分析

为解释 A3 算法在本场景下的强竞争力，分析了 RSRP 最强小区与 SINR 最高小区的一致率：

| 指标 | 值 |
|---|---|
| RSRP-SINR 一致率 | 72.8% |
| RSRP-SINR 相关系数 | 0.897 |
| RSRP 决策导致 SINR 损失的时隙比例 | 3.4% |
| 不一致时的平均 SINR 损失 | 12.3 dB |

**72.8% 的一致率**说明在大多数时隙，RSRP 最强的基站也是 SINR 最高的基站，A3 的 RSRP 决策在 96.6% 的时隙是正确的。这解释了 A3 在均值 SINR 上接近深度学习模型的原因。

**3.4% 的不一致时隙**中，SINR 损失高达 12.3 dB，这是深度学习模型的理论提升空间。Transformer 超过 A3 约 0.22 dB，与理论估算（$3.4\% \times 12.3 \approx 0.4$ dB）基本吻合（实际提升略小，因为深度学习模型并非在所有不一致时隙都做出正确决策）。

---

## 五、综合讨论

### 5.1 深度学习方法的优势

深度学习模型在均值 SINR 上超过 A3 基线，说明利用历史信道序列确实能够提升切换质量。Transformer 的 Self-Attention 机制能够直接建模任意时隙间的依赖关系，在短序列（$W=10$）场景下表现最优。

### 5.2 深度学习方法的代价

深度学习模型的切换次数（6.6-8.0 次）是 A3（2.8 次）的 2-3 倍，乒乓率（6.1-16.7%）是 A3（0.6%）的 10-28 倍。这说明深度学习模型在提升 SINR 质量的同时，引入了更多不必要的切换，增加了网络信令开销。

### 5.3 场景局限性

本实验在 7 基站低干扰场景下进行，RSRP-SINR 一致率高达 72.8%，A3 的 RSRP 决策已接近最优。在更复杂的场景（多基站、强干扰、RSRP-SINR 解耦明显）中，深度学习方法的优势预计会更加显著。

### 5.4 各模型适用场景

- **A3（优化后）**：适用于对切换稳定性要求高、信令开销敏感的场景，无需训练和推理资源
- **Transformer**：适用于对 SINR 质量要求高、推理时延敏感的场景，参数量最小（70K），推理最快（575 μs）
- **TCN**：适用于对切换稳定性有一定要求的场景，乒乓率最低（6.1%），但推理时延较高（2284 μs）
- **GRU**：综合性能居中，实现简单，适合作为深度学习切换决策的基线模型

---

## 六、结论

本文在慕尼黑市中心场景下，基于 Sionna RT 物理层仿真数据，对比了 A3 算法与三种深度学习切换策略（GRU、TCN、Transformer）的性能。主要结论如下：

1. **Transformer 在均值 SINR 上超过 A3 基线 0.22 dB**，验证了深度学习方法利用历史信道序列提升切换质量的可行性。

2. **A3 在切换效率和稳定性上仍然最优**（切换次数 2.8 次，乒乓率 0.6%），说明在少基站低干扰场景下，RSRP 决策已接近最优，深度学习方法的提升空间有限。

3. **RSRP-SINR 一致率（72.8%）是理解实验结果的关键**：高一致率使 A3 天然接近最优，深度学习方法的优势主要体现在 3.4% 的 RSRP-SINR 解耦时隙。

4. **TCN 在切换稳定性上表现最优**（乒乓率 6.1%），接近 A3 水平，说明卷积结构对时序模式的平滑建模有助于减少乒乓切换。

5. **Transformer 在推理效率上最优**（575 μs，参数量 70K），适合 UE 侧实时部署。

---

## 七、改进方案

### 7.1 仿真层面的改进

**7.1.1 增加场景复杂度（优先级：高）**

当前 7 基站低干扰场景中 RSRP-SINR 一致率高达 72.8%，限制了深度学习方法的优势空间。建议引入异构网络（宏站 + 微站）：

- 部署 3-4 个宏站（高功率，43 dBm，覆盖整体）+ 8-12 个微站（低功率，23-30 dBm，覆盖热点/弱覆盖区）
- 宏站 RSRP 强但干扰大，微站 RSRP 弱但 SINR 高，RSRP-SINR 解耦更明显
- 预期 RSRP-SINR 一致率降至 50-60%，深度学习方法的优势将更显著

**7.1.2 增加动态散射体**

当前场景为静态建筑物，不考虑行人和车辆的动态遮挡。可在仿真中加入移动散射体（如对向行驶的车辆），引入额外的信道时变性，使仿真更接近真实城市环境。

**7.1.3 扩大数据集规模**

当前 120 条轨迹（约 26 万样本）对于深度学习而言规模偏小。建议增加到 360-600 条轨迹，覆盖更多起点位置和轨迹类型，提高模型的泛化能力。可通过优化射线追踪参数（降低采样射线数至 $5 \times 10^5$）在可接受的精度损失下将仿真时间缩短约 50%。

---

### 7.2 模型层面的改进

**7.2.1 去掉 SINR 输入特征（优先级：高，学术严谨性）**

当前模型将 SINR 同时作为输入特征和评估指标，存在信息泄露风险。建议去掉输入中的 SINR，只保留 UE 可直接测量的特征（RSRP、RSRQ、Doppler、K_factor、DelaySpread），使模型学习从间接特征推断最优切换时机，提升学术严谨性和实际部署可行性。

**7.2.2 增加遮挡感知特征（优先级：中）**

当前特征中，K_factor 和 DelaySpread 已经包含了 LOS/NLOS 信息，但可以进一步增加：

- **RSRP 方差**（过去 $W$ 个时隙的标准差）：遮挡时 RSRP 波动大，边界时 RSRP 单调变化，方差能够区分两种情况
- **邻区 RSRP 趋势**（邻区 RSRP 的线性回归斜率）：边界时邻区 RSRP 上升，遮挡时邻区 RSRP 不变

这两个特征可以从已有的 `trajectory_data.npz` 中计算，无需重新仿真。

**7.2.3 双输出模型（优先级：中）**

当前模型只输出"目标小区 ID"，不区分"是否需要切换"和"切换到哪里"。建议改为双输出：

- **输出 1**：切换置信度（0-1），表示当前时刻是否值得切换
- **输出 2**：目标小区 ID（7 类分类）

只有置信度超过阈值时才执行切换，阈值在验证集上优化。这样模型能够主动学习"何时不切换"，减少不必要的切换次数。

**7.2.4 增大输入窗口（优先级：低）**

当前窗口 $W=10$（400ms）。对于 30 km/h 的低速 UE，建筑物遮挡的空间尺度约 10-30m，对应时间约 1-4 秒（25-100 个时隙）。增大窗口到 $W=25$（1s）可以让模型看到更完整的遮挡模式，但需要重新生成数据集。

---

### 7.3 切换策略层面的改进

**7.3.1 自适应滑动窗口（优先级：高）**

当前滑动窗口 $N=3$ 固定不变。建议根据模型预测的置信度动态调整：

- 置信度 > 0.95：$N=2$（快速切换，高置信度时减少延迟）
- 置信度 0.80-0.95：$N=3$（标准切换）
- 置信度 < 0.80：$N=5$（保守切换，低置信度时增加确认次数）

这样在模型确定的情况下减少切换延迟，在模型不确定的情况下减少误切换。

**7.3.2 基于 SINR 差值的切换门限（优先级：高，无需重训练）**

在现有模型基础上，增加切换门限：只有预测的目标小区 SINR 比当前小区高 $\delta$ dB 才执行切换：

$$\text{切换条件：} \hat{\gamma}_{\text{target}} - \hat{\gamma}_{\text{current}} > \delta$$

$\delta$ 在验证集上优化（类似 A3 的 offset 优化）。这一改进不需要重新训练模型，预期能将切换次数从 6-8 次降至 4-5 次，同时保持 SINR 质量。

**7.3.3 切换冷却时间自适应（优先级：低）**

当前切换冷却时间固定为 5 个时隙（200ms）。对于高速 UE，200ms 内可能已经移动到新的覆盖区域，冷却时间过长会错过最优切换时机。建议根据 UE 速度（由 Doppler 频移估算）动态调整冷却时间：

- 30 km/h：冷却 7 个时隙（280ms）
- 60 km/h：冷却 5 个时隙（200ms）
- 120 km/h：冷却 3 个时隙（120ms）

**7.3.4 强化学习方法（优先级：中，长期方向）**

监督学习的根本局限是需要定义"正确答案"（标签），而切换决策没有唯一正确答案。强化学习通过奖励信号（切换后 SINR 变化）直接优化目标，不需要标签：

- **状态**：当前时隙的信道特征（去掉 SINR）
- **动作**：切换到哪个小区（或保持当前）
- **奖励**：切换后 SINR 增益 - 切换代价 - RLF 惩罚

可采用离线强化学习（Conservative Q-Learning，CQL）直接从已有的 `trajectory_data.npz` 中学习策略，无需重新仿真。预期能够在减少切换次数的同时保持 SINR 质量，真正实现"更少切换达到更好 SINR"的目标。

---

### 7.4 改进优先级汇总

| 改进方向 | 优先级 | 预期效果 | 实现难度 | 是否需要重新仿真 |
|---|---|---|---|---|
| 去掉 SINR 输入 | 高 | 学术严谨性↑ | 低 | 否（重建数据集） |
| SINR 差值门限 | 高 | 切换次数↓ | 低 | 否（无需重训练） |
| 自适应滑动窗口 | 高 | 切换次数↓，SINR↑ | 低 | 否（无需重训练） |
| 增加遮挡感知特征 | 中 | SINR↑，乒乓率↓ | 中 | 否（重建数据集） |
| 双输出模型 | 中 | 切换次数↓ | 中 | 否（重建数据集） |
| 异构网络场景 | 高 | DL 优势↑↑ | 高 | 是（约 10 小时） |
| 强化学习 | 中 | 综合性能↑ | 高 | 否（离线 RL） |
| 增大输入窗口 | 低 | SINR↑（低速） | 中 | 是（重新生成数据） |
