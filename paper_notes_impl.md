# 实现细节（Implementation Details）

---

## 一、实验环境

实验在配备 NVIDIA RTX 4060 Ti（16GB 显存）的 Windows 11 工作站上进行。数据生成阶段使用 Sionna RT 2.0.1 进行物理层射线追踪仿真，运行时间约 298 分钟。模型训练阶段使用 PyTorch 2.x 在 GPU 上完成，三个模型合计训练时间约 10 分钟。模型评估阶段仅需 CPU，可在任意配置的计算机上运行。

---

## 二、数据集实现细节

### 2.1 仿真场景配置

仿真场景采用 Sionna RT 内置的慕尼黑市中心场景，该场景基于真实的 3D 建筑物几何数据，覆盖范围为 1km × 1km（坐标范围 ±500m）。场景中建筑物占地约 43.2%，可行走区域（街道）占 56.8%。

网络部署采用 7 个基站的非均匀布局，站间距约 307m，基站高度 10m（模拟路灯杆部署）。载波频率设置为 3.5 GHz，对应 5G NR Sub-6GHz 频段。天线配置为 SISO 全向天线，不考虑波束赋形，以聚焦于切换决策算法本身的性能差异。

### 2.2 UE 轨迹生成

共生成 120 条 UE 轨迹，按速度均匀分为三组：30 km/h（行人/自行车）、60 km/h（城市车辆）、120 km/h（快速路车辆），每组 40 条。轨迹类型包括沿街道网格移动、慕尼黑街道行走、圆弧转弯和停留-移动交替等多种模式，以覆盖不同的移动场景。

为确保轨迹起点位于街道而非建筑物内，首先从 Sionna 场景的 Mitsuba 网格中提取建筑物占地面积，生成分辨率为 2m 的可行走网格，轨迹起点从可行走区域中采样。对于中高速轨迹（>30 km/h），在每个时隙检测前方是否为建筑物，若是则通过沿墙滑行或转向机制绕过障碍，避免轨迹穿越建筑物。

每条轨迹的时隙时长为 40ms，轨迹长度根据速度自适应调整，目标覆盖约 4 个站间距的距离，确保每条轨迹经历多次切换事件。

### 2.3 信道仿真

每个时隙对每个 UE 位置独立执行射线追踪，计算 UE 与所有 7 个基站之间的多径信道。射线追踪参数设置为：最大反射次数 3 次，最大衍射次数 1 次，每个发射机的采样射线数为 $10^6$。

为加速仿真，利用邻区关系配置（每个基站平均 3.4 个邻区）减少射线追踪的基站数量，相比全量计算加速约 1.6 倍。

### 2.4 特征提取与归一化

从射线追踪结果中提取 10 类信道特征，每类对 7 个基站分别计算，共 70 维。RSRP 经过 L3 滤波（指数加权平均，滤波系数 $\alpha = 1/16$）以模拟真实网络中的测量滤波过程。

各特征的归一化方式如下：RSRP 和 RSRQ 采用 min-max 归一化，归一化范围分别为 $[-140, -40]$ dBm 和 $[-30, 0]$ dB；SINR 归一化范围为 $[-20, 40]$ dB；Doppler 频移采用对称归一化，范围 $\pm 500$ Hz；时延扩展、K 因子和最短路径时延采用正值归一化，分别除以 $10^{-6}$ s、30 和 $2\times10^{-6}$ s。所有归一化后的值通过 clip 操作限制在合理范围内。

### 2.5 标签生成

标签定义为切换收益最大的动作：对每个时隙，计算切换到每个邻区在未来 10 个时隙（400ms）内的平均 SINR 增益，减去切换代价 3 dB。若所有邻区的切换收益均不超过 0，则标签为保持当前小区。这一设计使标签直接反映"是否值得切换"，而非简单地预测"哪个小区信号最强"。

### 2.6 数据集划分

按轨迹进行分层划分，训练集、验证集、测试集的比例为 60:20:20，分别包含 72、24、24 条轨迹。分层划分确保每种速度在各划分中的比例相同。最终数据集包含约 25.6 万个样本，每个样本为 10 个连续时隙的特征序列（形状 $10 \times 70$）及对应的切换动作标签。

---

## 三、A3 算法实现细节

### 3.1 参数初始化

所有邻区对的切换偏置初始化为 3.0 dB，这是 3GPP 标准推荐的默认值。TTT 设置为 80ms（对应 2 个时隙），迟滞量设置为 2 dB，切换冷却时间设置为 5 个时隙（200ms）。

### 3.2 邻区关系配置

邻区关系从 `network_config.json` 中读取，该文件由 Sionna RT GUI 工具在基站部署阶段生成。本实验共有 42 个有向邻区对，平均每个基站有 3.4 个邻区。邻区关系的配置不仅用于 A3 算法的 offset 优化，也用于射线追踪阶段的加速（只对服务小区和邻区执行射线追踪，跳过非邻区基站）。

### 3.3 Offset 优化过程

对每个邻区对，在 $[-3.0, 9.0]$ dB 的范围内以 0.5 dB 为步长进行网格搜索，共 25 个候选值。优化采用单轮贪心坐标下降：按邻区关系顺序依次优化每个邻区对，每次优化时其他邻区对保持当前最优值不变。由于不同邻区对之间的切换事件较少重叠，单轮优化已能获得较好结果，无需迭代至收敛。

优化目标为综合得分，包含三项：加权均值 SINR（权重 1.0）、每轨迹平均切换次数（惩罚系数 0.1）和总 RLF 次数（惩罚系数 5.0）。RLF 的惩罚系数远大于切换次数，反映了无线链路失败对用户体验的严重影响。

优化完成后，将最优 offset 缓存到文件，后续评估直接加载，无需重复优化。

### 3.4 RLF 判定

当服务小区的 SINR 持续低于 -6 dB 超过 200ms（5 个时隙）时，判定为一次 RLF 事件。该阈值参考 3GPP 标准中的无线链路失败判定条件。

---

## 四、深度学习模型实现细节

### 4.1 模型架构参数

**GRU**：隐状态维度 128，2 层，单向，层间 Dropout 率 0.3。输入经过 GRU 处理后，取最后时隙的隐状态，经过 Dropout 和全连接层输出 7 维 logits。

**TCN**：输入首先经过线性投影层将 70 维特征映射到 64 维，然后转置为 channel-first 格式送入 TCN 块。三个 TCN 块的通道数依次为 64、128、128，膨胀因子依次为 1、2、4，卷积核大小为 3。每个 TCN 块包含两层因果膨胀卷积，每层后接 BatchNorm、ReLU 和 Dropout（率 0.2），最后通过残差连接与输入相加。取最后时刻的特征向量，经过 Dropout 和全连接层输出 7 维 logits。

**Transformer**：输入经过线性投影层映射到 64 维，在序列开头拼接一个可学习的 CLS token（初始化为截断正态分布，标准差 0.02），加上正弦位置编码后送入 2 层 Transformer Encoder。每层使用 4 个注意力头（每头维度 16），前馈网络维度 128，Dropout 率 0.1，采用 Pre-LN（先归一化再注意力）结构。因果掩码确保每个时隙只能看到当前及之前的时隙，CLS token 可以看到所有位置。取 CLS token 的输出，经过 LayerNorm、Dropout 和全连接层输出 7 维 logits。

### 4.2 训练配置

所有模型使用相同的训练配置。优化器选用 AdamW，初始学习率 $10^{-3}$，权重衰减 $10^{-4}$。学习率调度采用余弦退火，最小学习率为初始值的 1%（即 $10^{-5}$），在最大轮数内完成一个完整的余弦周期。

批大小设置为 512，最大训练轮数为 50。为防止梯度爆炸，每步更新前对梯度进行裁剪，最大范数为 1.0。采用 Early Stopping 策略，当验证集准确率连续 10 轮不提升时停止训练，保存验证集准确率最高的模型权重。

### 4.3 类别权重

由于标签分布不均衡（cell_0 和 cell_2 各占约 31%，其余各占 5-10%），训练时对损失函数施加类别权重。权重计算方式为 $w_c = N / (C \cdot N_c)$，其中 $N$ 为总样本数，$C$ 为类别数，$N_c$ 为第 $c$ 类的样本数，归一化使所有权重的均值为 1。这一设计使模型对少数类（如 cell_1、cell_3）给予更多关注，避免模型偏向多数类。

### 4.4 特征归一化时机

训练数据集（`dataset.npz`）中存储的是原始物理量（未归一化），在训练前通过检测特征范围（RSRP 原始值约 -155 dBm，远低于 -10）自动判断是否需要归一化，并应用与 `get_feature_matrix()` 完全一致的归一化参数。这确保了训练时的特征分布与推理时（从轨迹数据实时计算特征）的分布一致，避免训练-推理不一致导致的性能下降。

### 4.5 训练时间

在 RTX 4060 Ti 上，GRU 训练约 44 秒（8 个 epoch，Early Stopping），TCN 训练约 77 秒（12 个 epoch），Transformer 训练约 151 秒（32 个 epoch）。Transformer 收敛较慢，但最终达到最高的验证集准确率（94.9%）。

---

## 五、切换执行策略实现细节

### 5.1 滑动窗口参数

深度学习模型每个时隙输出预测的目标小区，但不立即执行切换。采用滑动窗口机制：当模型连续 3 次（120ms）预测同一邻区优于当前服务小区时，才执行切换。这一设计类比 A3 的 TTT 机制，过滤单次预测误差，减少乒乓切换。

切换执行后设置 5 个时隙（200ms）的冷却时间，期间不再触发新的切换，与 A3 的冷却时间保持一致，确保对比的公平性。

### 5.2 初始服务小区

轨迹开始时，初始服务小区选择第一个时隙 RSRP 最强的基站，与 A3 算法的初始化方式相同。

### 5.3 批量推理优化

为提高评估效率，对每条轨迹的所有时隙进行批量推理：预先构建所有时隙的特征窗口（对 $t < W$ 的时隙用零填充），一次性送入模型得到所有时隙的预测结果，再按时序执行滑动窗口逻辑。这避免了逐时隙推理的重复开销，显著加快评估速度。

---

## 六、推理时延测量

推理时延测量针对单个样本（批大小为 1），模拟 UE 侧实时推理的场景。测量流程为：首先进行 10 次预热推理（不计入统计），然后正式测量 100 次，取均值作为最终结果。对于 GPU 推理，每次测量前后调用 `torch.cuda.synchronize()` 确保 GPU 操作完成。

本实验在 CPU 上测量推理时延，结果为：GRU 572 μs，TCN 2284 μs，Transformer 575 μs。TCN 推理时延显著高于其他两种模型，主要原因是 BatchNorm 在推理时需要使用训练集统计量（running mean/var），且卷积操作的内存访问模式不如矩阵乘法高效。GRU 和 Transformer 的推理时延相近，均在 600 μs 以内，满足 UE 侧实时决策的时延要求（切换决策需在一个时隙 40ms 内完成）。

---

## 七、评估指标实现细节

### 7.1 均值 SINR 和 P5 SINR

对每条测试轨迹，根据切换策略确定的服务小区序列，提取每个时隙的服务小区 SINR，计算均值和第 5 百分位数。最终结果为所有测试轨迹的 SINR 序列拼接后的统计值，而非各轨迹均值的平均，这样能更准确地反映整体分布。

### 7.2 RLF 判定

遍历每条轨迹的服务小区 SINR 序列，统计 SINR 持续低于 -6 dB 超过 5 个时隙（200ms）的事件次数。采用状态机实现：进入低 SINR 状态后开始计时，持续时间达到阈值时计为一次 RLF，并标记为"已在 RLF 中"，避免同一次 RLF 事件被重复计数。

### 7.3 乒乓率

对每次切换事件，检查在随后 25 个时隙（1s）内是否切换回原服务小区。乒乓率定义为发生乒乓的切换次数占总切换次数的比例。若轨迹中没有发生任何切换，乒乓率定义为 0。

### 7.4 A3 offset 缓存

A3 的 offset 优化在训练集上进行，耗时约 9.6 分钟（42 个邻区对 × 25 个候选值 × 72 条轨迹）。优化完成后将结果保存为 JSON 文件，后续评估直接加载，无需重复优化。这确保了每次评估使用相同的 A3 配置，结果可复现。